# 📱 دليل تثبيت APK على التابلت

> خطوة بخطوة: من تنزيل الـ APK إلى تشغيل التطبيق.

---

## الخطوة 1: السماح بتثبيت التطبيقات من خارج Google Play

بسبب سياسات الأمان، أندرويد يسألك قبل تثبيت أي APK من خارج المتجر.

### على Android 8.0+ (الأغلبية):
1. افتح `Settings` (الإعدادات)
2. `Apps & notifications` (التطبيقات والإشعارات)
3. `Special access` (وصول خاص)
4. `Install unknown apps` (تثبيت تطبيقات غير معروفة)
5. اختر التطبيق اللي نازل منه الـ APK (مثلاً `Chrome` أو `Files`)
6. فعّل `Allow from this source` (السماح من هذا المصدر)

### على Android 7.0 وأقدم:
1. `Settings` → `Security`
2. فعّل `Unknown sources`

---

## الخطوة 2: إيجاد ملف الـ APK

### إذا نزلته من GitHub:
- افتح تطبيق **Files** (مدير الملفات)
- اذهب إلى `Downloads`
- ستجد ملف مضغوط `khayrity-debug-apk.zip`
- **فك الضغط** أولاً (اضغط عليه ثم "Extract")
- تجد داخله `app-debug.apk`

### إذا نزلته مباشرة:
- تجد `app-debug.apk` في مجلد Downloads

---

## الخطوة 3: التثبيت

1. اضغط على `app-debug.apk`
2. تظهر شاشة: "Do you want to install this application?"
3. اضغط `INSTALL` (أو `تثبيت`)
4. انتظر 5-10 ثواني
5. اضغط `OPEN` (فتح) أو `DONE` (تم)

---

## الخطوة 4: أول تشغيل

1. التطبيق اسمه **خريطتي** (Khayrity)
2. أول مرة يفتح، يعطيك شاشة فارغة: "لا توجد خرائط بعد"
3. اضغط زر `+` لإنشاء أول خريطة
4. جرّب الإيماءات:
   - **ضغطة مطوّلة** على مكان فارغ → إضافة عقدة
   - **سحب بإصبعين** → تحريك وتكبير اللوحة
   - **الضغط على عقدة** → فتح محرر العقدة

---

## 🔄 تحديث التطبيق لاحقاً

عند بناء نسخة جديدة:

1. نزّل الـ APK الجديد
2. اضغط عليه
3. أندرويد يسألك: "تثبيت تحديث؟" → اضغط `INSTALL`
4. **بياناتك ستبقى** (نفس قاعدة البيانات)

---

## ❌ إلغاء التثبيت

1. اضغط مطوّلاً على أيقونة **خريطتي**
2. اختر `Uninstall` (إلغاء التثبيت)
3. أو من: `Settings` → `Apps` → `خريطتي` → `Uninstall`

---

## 🐛 مشاكل شائعة

### "App not installed" (التطبيق غير مثبت)
- **السبب:** إصدار قديم مثبت من قبل
- **الحل:** احذف النسخة القديمة أولاً، أو اضغط `Update` بدل `Install`

### "Parse error" (خطأ في التحليل)
- **السبب:** الـ APK تالف (تنزيل ناقص)
- **الحل:** احذف الملف ونزّله مرة ثانية

### "There was a problem parsing the package"
- نفس الحل أعلاه

### التطبيق يفتح ويغلق فوراً
- **السبب:** خطأ في التطبيق نفسه
- **الحل:** راسلني بالخطأ وسأصلحه

---

## 📋 معلومات تقنية عن الـ APK

| الخاصية | القيمة |
|---|---|
| اسم التطبيق | خريطتي (Khayrity) |
| Package ID | com.mindmap.khayrity.debug |
| الإصدار | 1.0.0-debug |
| الحجم | ~15-20 MB |
| minSdk | 24 (Android 7.0) |
| targetSdk | 35 (Android 15) |
| نوع البناء | Debug (غير مُحسَّن) |

> 💡 **نصيحة:** للنسخة النهائية (Release)، الـ APK يكون ~10MB ومحسَّن أكثر. تأتي مع `R8/ProGuard` لتقليل الحجم وإخفاء الكود.

---

استمتع بتجربة خريطتي! 🧠✨
